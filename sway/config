# ----------------------------------------------------------------------
# Configuración Principal de Sway - John-NCC
# ----------------------------------------------------------------------

# Tecla modificadora (Windows / Super)
set $mod Mod4

# Terminal preferida (Kitty)
set $term kitty

# Lanzador de aplicaciones (Rofi)
set $menu rofi -show drun

# Movimiento de ventanas flotantes con el ratón + Mod
floating_modifier $mod normal

# El foco sigue al ratón (comportamiento clásico de tiling WMs)
focus_follows_mouse yes

# ----------------------------------------------------------------------
# Inclusiones
# ----------------------------------------------------------------------
# Cargar configuración predeterminada del sistema (si existe)
include /etc/sway/config.d/*

# Cargar tus módulos personalizados
include config.d/*
